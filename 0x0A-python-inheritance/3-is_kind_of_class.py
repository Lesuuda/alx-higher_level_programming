#!/usr/bin/python3
"""Returns True if obj is exactly an instance of the specified class;
otherwise False."""

def is_kind_of_class(obj, a_class):
    """function to test is instance"""
    return isinstance(obj, a_class)
